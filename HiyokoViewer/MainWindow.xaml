<Window x:Class="HiyokoViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HiyokoViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- ボタン用の行 -->
            <RowDefinition Height="*"/>    <!-- 画像表示用の行 -->
        </Grid.RowDefinitions>

        <!-- 上部のコントロール -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="ファイルを開く" Width="100" Click="OpenFileButton_Click"/>
        </StackPanel>

        <!-- 画像表示エリア -->
        <!-- Borderで囲むと背景色がわかりやすい -->
        <Border Grid.Row="1" Background="Gray" Margin="5">
            <Image x:Name="MainImage"/>
        </Border>

    </Grid>
</Window>
